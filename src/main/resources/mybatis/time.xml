<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.time.TimeDAOInter">
<insert id="create" parameterType="dev.mvc.calendar.CalendarVO">
    INSERT INTO time(timeno, labeldate, rdate, galleryno, memberno)
    VALUES(time_seq.nextval, #{labeldate} sysdate, #{galleryno}, #{memberno})
  </insert>

</mapper>